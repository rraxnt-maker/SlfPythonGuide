"""
Arrays store elements in contiguous memory locations, resulting in easily calculable addresses for the elements stored and this allows faster access to an element at a specific index. Linked lists are less rigid in their storage structure and elements are usually not stored in contiguous locations, hence they need to be stored with additional tags giving a reference to the next element. This difference in the data storage scheme decides which data structure would be more suitable for a given situation.
"""

class Узел:
    def __init__(self, данные):
        self.данные = данные
        self.следующий = None

class СвязныйСписок:
    def __init__(self):
        self.голова = None

    def добавить_в_конец(self, данные):
        новый_узел = Узел(данные)

        if self.голова is None:
            self.голова = новый_узел
            return

        текущий = self.голова
        while текущий.следующий is not None:
            текущий = текущий.следующий

        текущий.следующий = новый_узел

    def показать(self):
        элементы = []
        текущий = self.голова
        while текущий is not None:
            элементы.append(текущий.данные)
            текущий = текущий.следующий
        print(" → ".join(элементы))

# Использование
список = СвязныйСписок()
список.добавить_в_конец("Аня")
список.добавить_в_конец("Боря")
список.добавить_в_конец("Вова")
список.показать()  # Аня → Боря → Вова
